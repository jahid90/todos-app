{
    "license": "ISC",
    "scripts": {
        "build": "./mvnw package",
        "package": "docker build -t todos.service:local .",
        "rebuild": "yarn build && yarn package",
        "deploy": "docker-compose up -d",
        "shutdown": "docker-compose down"
    }
}
