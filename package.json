{
    "license": "ISC",
    "scripts": {
        "build": "./mvnw package",
        "start": "java -jar target/*.jar",
        "docker:build": "docker build -t todos.service:local .",
        "docker:start": "docker-compose up -d",
        "docker:stop": "docker-compose down"
    }
}
